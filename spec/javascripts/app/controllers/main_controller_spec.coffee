#= require spec_helper

# describe 'NflTeamsCtrl', ->
#   beforeEach ->
#     @controller('NflTeamsCtrl', { $scope: @scope })
#     # @Task = @model('Task')
#     # @tasks = [new @Task({ id: 1, name: 'first task' })]

#     # @http.whenGET('/api/tasks').respond(200, @tasks)
#     # @http.flush()

#   describe 'load', ->
#     it 'opens up the main controller', ->
#       expect(1).toEqual(1)
